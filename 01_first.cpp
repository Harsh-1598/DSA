#include<iostream> //It gives us cin and cout for input and output.
using namespace std; // without this line we have to write std:: everywhere before writing the default functions.
int main(){
    cout <<"hello world!"<<endl<<"I am Harsh Kejriwal"<<endl;
    // cout: prints in the screen
    // In the cout and cin you can write many lines just adding the << and use next line in two ways: 
    // 1. by using the \n and
    // 2. by writing the endl in the end of each line and also it should be written after the << 
    return 0;
}